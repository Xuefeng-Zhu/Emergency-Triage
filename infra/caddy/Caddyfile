:8080 {
	encode zstd gzip

	@websocket path /ws/*
	reverse_proxy @websocket realtime:3001

	reverse_proxy web:3000

	header {
		-Server
		X-Content-Type-Options nosniff
		Referrer-Policy no-referrer
		Permissions-Policy "camera=(), geolocation=(), payment=()"
	}
}
